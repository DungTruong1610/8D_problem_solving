# Cùng thiết lập với .npmrc ở thư mục gốc repo. Vercel cài đặt trực tiếp trong
# thư mục này (Root Directory = app/8D_hackathon_ui) nên nó KHÔNG đọc .npmrc ở
# gốc — thiếu dòng dưới đây thì npm chạy chế độ peer-deps nghiêm ngặt, khác với
# cách package-lock.json được sinh ra.
legacy-peer-deps=true
